:80 {
	root * /usr/share/caddy
	encode gzip
	# SPA fallback: client-side routes (react-router) resolve to index.html so
	# deep links (e.g. /projects) work on hard refresh instead of 404ing.
	try_files {path} /index.html
	file_server
}
