{
  "name": "backend-rust",
  "version": "1.0.0",
  "private": true,
  "description": "Rust implementation of the iota-terminal API contract. Scripts shell out to the native Cargo toolchain.",
  "scripts": {
    "build": "cargo build --release",
    "dev": "cargo run",
    "lint": "cargo clippy -- -D warnings"
  }
}
